export async function POST(req:Request){try{const {text,voice_id='eve',language='en'}=await req.json();if(!text?.trim())return Response.json({error:'Text kosong.'},{status:400});const r=await fetch('https://api.x.ai/v1/tts',{method:'POST',headers:{Authorization:`Bearer ${process.env.XAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({text,voice_id,language})});if(!r.ok)return Response.json({error:await r.text()||`TTS error ${r.status}`},{status:r.status});return new Response(await r.arrayBuffer(),{headers:{'Content-Type':r.headers.get('content-type')||'audio/mpeg','Cache-Control':'no-store'}})}catch(e){return Response.json({error:e instanceof Error?e.message:'TTS server error.'},{status:500})}}
