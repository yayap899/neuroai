'use client';
import {useEffect,useRef} from 'react';
import * as THREE from 'three';
export default function Starfield(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!ref.current)return;const el=ref.current,scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(70,innerWidth/innerHeight,.1,1000);camera.position.z=5;const renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);el.appendChild(renderer.domElement);const g=new THREE.BufferGeometry(),n=2600,p=new Float32Array(n*3);for(let i=0;i<n*3;i+=3){p[i]=(Math.random()-.5)*150;p[i+1]=(Math.random()-.5)*100;p[i+2]=(Math.random()-.5)*120}g.setAttribute('position',new THREE.BufferAttribute(p,3));const m=new THREE.PointsMaterial({color:0x63adff,size:.055,transparent:true,opacity:.8});const stars=new THREE.Points(g,m);scene.add(stars);let id=0;const animate=()=>{id=requestAnimationFrame(animate);stars.rotation.y+=.00032;stars.rotation.x+=.00006;renderer.render(scene,camera)};animate();const resize=()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)};addEventListener('resize',resize);return()=>{cancelAnimationFrame(id);removeEventListener('resize',resize);g.dispose();m.dispose();renderer.dispose();el.removeChild(renderer.domElement)}} ,[]);return <div className="starfield" ref={ref}/>}
